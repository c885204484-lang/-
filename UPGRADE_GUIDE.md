# 桌面宠物升级指南

## 相比原版代码的主要改动

这次升级保留了原来的 STM32F103C8T6、5 路舵机、OLED、双串口、LED、电池检测电路以及 `0x29` 到 `0x50` 控制命令。主要变化集中在程序运行方式、外设协作能力、安全保护和可配置性。

### 改动总览

| 项目 | 原版代码 | 升级后的代码 |
|---|---|---|
| 主循环 | 持续调用动作函数和 OLED 刷新函数 | 根据 1ms 系统时基分周期调度不同任务 |
| 动作控制 | 动作函数内部大量使用 `Delay_ms()` 和循环 | 使用非阻塞关键帧状态机，每 20ms 更新一次 |
| 舵机运动 | 直接跳到指定角度 | 从当前角度平滑插值到目标角度 |
| 串口中断 | 在中断中读取并解析命令、修改动作状态 | 中断只收取字节，主循环解析环形缓冲区 |
| 通信协议 | 只支持单字节命令 | 保留单字节命令，并增加数据帧、校验和及状态回复 |
| 电量采样 | TIM3 中断中同步读取 ADC，再进行 100 次累加平均 | TIM3 触发 ADC，DMA 自动循环采样，再进行两级平均 |
| OLED | 主循环每次都清屏、绘图和更新 | 只有表情、电量或显示状态变化时才刷新 |
| LED | 在 TIM3 中断里更新呼吸灯 | 由调度任务定期更新，不再占用 TIM3 中断服务函数 |
| 宠物行为 | 只根据命令切换动作和表情 | 增加心情、精力、活跃度、睡眠和健康状态 |
| 参数保存 | 断电后参数恢复为代码默认值 | 使用内部 Flash 保存设置，并通过硬件 CRC 校验 |
| 故障恢复 | 程序卡死后只能人工复位 | 增加独立看门狗，并记录是否发生过看门狗复位 |
| CPU 空闲 | 主循环持续运行 | 无任务时执行 `WFI` 等待中断 |

### 1. 新增裸机任务调度器

原版 `main()` 的核心逻辑是：

```text
PetAction_Perform()
Face_Config()
```

如果一个动作内部需要运行数秒，主循环就会长时间停留在动作函数中。升级后新增 `Scheduler.c/.h`，使用 SysTick 产生 1ms 系统时基，并在主循环中调度：

- 每 20ms：更新动作关键帧和舵机插值。
- 每 100ms：更新尾巴和 LED 行为。
- 每 200ms：检查是否需要刷新 OLED；睡眠时延长到 1000ms。
- 每 500ms：处理一次经过 DMA 平均后的电池数据。
- 每 1000ms：更新宠物状态并发送遥测；睡眠时状态更新和遥测都延长到每 5000ms 一次。

这样能够让动作、显示、电量、串口和灯光在同一时间共同运行。

### 2. 将阻塞动作改成非阻塞关键帧动作

原版 `PetAction.c` 在前进、转向、摇摆和招手等函数中使用多层 `while`、`for` 和 `Delay_ms()`。升级后：

- 每个动作被定义为若干组“5 路目标角度 + 持续时间”的关键帧。
- `PetAction_Task20ms()` 只处理当前关键帧的一小步，然后立即返回主循环。
- 收到停止、新动作或低电量事件时，可以立即替换当前动作，不需要等待原动作的延时结束。
- 前进、后退、左转和右转仍支持蓝牙持续运动。
- 语音命令和招手、跳跃、伸懒腰等动作仍按单次动作处理，不会因为来自蓝牙通道而无限重复。

动作编号和原控制命令保持兼容，但动作轨迹改成了平滑插值，因此实际节奏可能和原版略有差异。烧录后应架空机器人测试，并重新确认每路舵机的机械限位。

### 3. 增加舵机平滑控制与校准

`Servo.c` 除了保留原来的单路角度控制函数，还增加了：

- 5 路舵机当前位置和目标角度记录。
- 根据动作持续时间自动计算每 20ms 的角度变化量。
- 每路舵机独立的最小角度、最大角度和零点微调值。
- 查询当前角度和目标角度的接口，用于蓝牙遥测。

校准参数可以通过 `0x64` 命令修改，再通过 `0x61` 保存到 Flash。

### 4. 重构双串口接收方式

原版在 `USART1_IRQHandler()` 和 `USART3_IRQHandler()` 中直接执行一长串命令判断，而且同一个接收寄存器会被重复读取。升级后：

- USART1 和 USART3 分别使用独立的 64 字节环形缓冲区。
- 中断服务函数只读取一次数据寄存器并将字节入队。
- 命令判断、数据帧解析和动作请求全部放到主循环执行。
- 缓冲区满时记录溢出次数。
- 错误校验和、超长数据区和超过 100ms 的不完整帧都会被丢弃，不会触发动作。
- 蓝牙会周期性发送电量、宠物状态、动作、舵机目标角度和错误计数。

### 5. 将电量采样升级为定时器、ADC 和 DMA 联动

原版每 20ms 在 TIM3 中断中调用 ADC 软件转换并等待转换结束。升级后：

- TIM3 更新事件直接触发 ADC1 通道 4 采样。
- DMA1 通道 1 自动把 ADC 结果写入 32 点循环缓冲区。
- CPU 不需要在中断里等待 ADC 转换完成。
- 程序先计算 DMA 缓冲区的 32 点平均值，再对最近 8 次电量结果进行滑动平均。

这部分同时使用了定时器触发、ADC 和 DMA，是本次升级中最典型的多外设联动。

### 6. 增加虚拟宠物状态

新增 `PetState.c/.h`，维护以下状态：

- `mood`：心情，范围为 0 到 100。
- `energy`：精力，范围为 0 到 100。
- `activity`：活跃度，影响尾巴摆动速度。
- `sleeping`：是否处于睡眠状态。
- `health`：正常、低电量或精力不足。
- 最后互动时间和开机运行秒数。

主要联动行为包括：

- 收到有效命令时唤醒宠物并提高心情。
- 运动过程中消耗精力，静止时恢复精力。
- 120 秒没有互动时自动趴下睡觉。
- 心情较高时显示快乐表情并摇尾巴。
- 电量或精力不足时拒绝跳跃和剧烈摇摆。

### 7. 增加低电量保护

原版只显示电量，不会主动干预动作。升级后，当 `CurBattery` 大于 0 且不高于 `15` 时：

- 健康状态切换为低电量。
- 关闭两路 LED，降低额外耗电。
- 中断正在执行的高耗能动作并进入趴下姿态。
- 禁止跳跃和剧烈摇摆类动作。
- 通过蓝牙状态帧上报低电量健康状态。

当前 `15` 按现有换算公式约对应 3.15V，必须根据实际分压电阻和万用表测量结果校准。

### 8. 增加 Flash 配置保存和硬件 CRC

新增 `ConfigStore.c/.h`，使用 STM32 内部 Flash 最后一个 1KB 页面 `0x0800FC00` 保存：

- 动作速度、摇摆延时。
- LED、呼吸灯和电量显示设置。
- 心情和精力。
- 5 路舵机的最小角度、最大角度和零点微调值。

读取时会检查魔数、参数范围和 STM32 硬件 CRC。数据损坏或从未保存时，程序使用默认参数，不会直接采用无效数据。

### 9. 增加独立看门狗和低功耗等待

新增 `Watchdog.c/.h`：

- 独立看门狗在程序异常卡死、主循环无法继续喂狗时自动复位 MCU。
- 上电后读取复位标志，并在蓝牙状态帧中返回看门狗复位标志。
- 正常主循环每轮喂狗。

主循环空闲时执行 `__WFI()`，让 Cortex-M3 等待 SysTick、USART 等中断；宠物睡眠后还会降低 OLED 和遥测任务频率。

### 10. 修改和新增的主要文件

新增文件：

- `Scheduler.c/.h`：1ms 时基和周期任务判断。
- `PetState.c/.h`：虚拟宠物状态及多外设联动策略。
- `ConfigStore.c/.h`：Flash 配置保存、读取和 CRC 校验。
- `Watchdog.c/.h`：独立看门狗和复位原因记录。

主要修改文件：

- `main.c`：改为非阻塞周期调度主循环。
- `PetAction.c/.h`：改为关键帧动作状态机。
- `Servo.c/.h`：增加插值、目标角度和校准功能。
- `BlueTooth.c/.h`：增加环形缓冲、数据帧协议和遥测。
- `AD.c/.h`、`PowerDetection.c`：改为定时器触发 ADC、DMA 循环采样和多级平均。
- `PWM.c`：让 TIM3 输出 ADC 触发信号，不再承担周期中断任务。
- `Face_Config.c`：仅在显示内容变化时更新 OLED。
- `Project.uvprojx`：将全部新增源码加入 Keil 工程。

### 11. 保持不变或兼容的部分

- MCU 仍为 STM32F103C8T6，继续使用标准外设库和 Keil 工程。
- 舵机、OLED、LED、语音模块、蓝牙模块及电量检测引脚没有改变。
- 原来的 `0x29` 到 `0x50` 单字节命令继续有效。
- OLED 表情图片和字模数据没有替换。
- 仍未启用 MPU6050，也未增加对外部 RTC 晶振的强制依赖。

## 运行架构

- SysTick 提供 1ms 系统时基。
- 舵机关键帧任务每 20ms 运行一次，不再使用阻塞延时。
- 尾巴和 LED 行为任务每 100ms 运行一次。
- OLED 仅在显示状态发生变化时刷新。
- TIM3 定时触发 ADC1 采集电池数据，DMA1 通道 1 自动搬运采样结果。
- USART 中断只负责将接收到的字节放入环形缓冲区，协议解析在 `main()` 主循环中执行。
- CPU 空闲时执行 `WFI` 等待中断；宠物进入睡眠状态后，会降低 OLED 和蓝牙遥测的刷新频率。

## 蓝牙通信协议

原有的 `0x29` 到 `0x50` 单字节命令仍然兼容。

新的数据帧格式如下：

```text
AA 55 CMD LEN DATA... CHECKSUM
```

各字段含义：

- `AA 55`：帧头。
- `CMD`：命令字节。
- `LEN`：数据区长度。
- `DATA`：可选的命令参数。
- `CHECKSUM`：前面所有字节累加和的低 8 位。

如果一个数据帧在 100ms 内没有接收完整，解析器会丢弃该帧并恢复到等待帧头的状态。

新增命令：

| 命令 | 数据区 | 功能 |
|---|---|---|
| `0x60` | 无 | 查询宠物当前状态 |
| `0x61` | 无 | 保存当前配置和宠物状态到内部 Flash |
| `0x62` | 无 | 停止当前动作并恢复站立姿态 |
| `0x63` | 速度值除以 10、摇摆延时 | 设置动作速度参数 |
| `0x64` | 舵机编号、最小角度、最大角度、有符号微调值 | 设置单路舵机校准参数 |

返回数据：

- `0x7F`：命令执行结果，数据为 `1` 表示成功，为 `0` 表示命令被拒绝。
- `0x70`：状态遥测帧，数据依次为电量、心情、精力、活跃度、当前动作、健康状态、睡眠状态、5 路舵机目标角度、协议错误计数和看门狗复位标志。

例如，查询状态命令 `0x60` 的完整数据帧为：

```text
AA 55 60 00 5F
```

其中校验和为 `(0xAA + 0x55 + 0x60 + 0x00) & 0xFF = 0x5F`。

## 配置持久化

程序使用内部 Flash 的最后一个 1KB 页面保存配置，起始地址为 `0x0800FC00`。

保存内容包括：

- 动作速度和摇摆延时。
- LED 开关和呼吸灯设置。
- 电量显示开关。
- 宠物的心情和精力。
- 5 路舵机的最小角度、最大角度和零点微调值。

配置记录使用 STM32 硬件 CRC 外设进行校验。如果魔数、参数范围或 CRC 校验不正确，程序会放弃该记录并恢复为保守的默认配置。

除非同时修改配置存储地址或链接布局，否则不要让应用程序代码占用到最后一个 Flash 页面。

Flash 有擦写寿命限制，因此 `0x61` 保存命令只应在参数确实发生变化后使用，不要高频、周期性发送。

## 宠物状态与联动规则

- 长时间没有收到互动命令时，宠物会进入睡眠状态并降低任务刷新频率。
- 执行运动动作会逐渐消耗精力，休息时会恢复精力。
- 心情会在收到互动命令时提高，在长时间无人互动时逐渐降低。
- 电量过低时会关闭 LED、禁止跳跃和剧烈摇摆，并进入低功耗保护姿态。
- 精力过低时会拒绝跳跃等高耗能动作。
- 心情较高且处于清醒状态时，会显示快乐表情并允许自动摇尾。

## 实机检查步骤

1. 首次测试时将机器人架空，防止腿部动作将机器人推下桌面。
2. 逐路确认舵机方向，并优先设置保守的最小角度和最大角度，避免机械结构卡死。
3. 使用万用表测量电池电压，对照程序显示值验证电压换算公式。
4. 确认低电量阈值是否适合实际电池。当前阈值为 `15`，按照现有计算方式约表示 3.15V。
5. 在每一种长动作执行过程中发送停止命令，确认机器人能够立即中断动作并站立。
6. 发送不完整数据帧、错误长度和错误校验和的数据帧，确认机器人不会误执行动作。
7. 修改速度、灯光或舵机校准参数，发送 `0x61`，然后断电重启，确认配置能够恢复。
8. 在调试版本中故意让程序停止喂狗，等待看门狗复位，然后查询状态，确认看门狗复位标志被正确上报。
9. 连续运行数小时，观察舵机、OLED、蓝牙、电量采样和睡眠唤醒是否保持正常。

## 暂未启用的功能

当前版本没有启用 MPU6050 和真实日历时间功能，因为现有硬件信息无法确认已经安装 MPU6050，也无法确认 RTC 使用的外部低速晶振是否存在。

后续增加 MPU6050 时，可以在现有架构中加入独立的姿态采集任务和姿态事件，而不需要重新改回阻塞式动作控制。
